import "@/styles/loading.scss";

const Loading = () => {
    return (
        <main class="loading">
            <div></div>
            <p>Loading...</p>
        </main>
    );
}

export default Loading;